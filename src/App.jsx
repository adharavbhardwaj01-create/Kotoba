import React, { useState, useEffect, useRef, useCallback } from "react";/* ------------------------------------------------------------------ *//* Fonts + global style                                                *//* ------------------------------------------------------------------ */const GlobalStyle = () => (  <style>{`    @import url('https://fonts.googleapis.com/css2?family=Shippori+Mincho:wght@500;700;800&family=Zen+Kaku+Gothic+New:wght@400;500;700;900&family=JetBrains+Mono:wght@400;500;600&display=swap');    * { box-sizing: border-box; }    .adharav-root {      --paper: #efe7d8;      --paper-dim: #e4dac5;      --ink: #22252b;      --ink-soft: #55564f;      --indigo: #2c4770;      --indigo-deep: #1c2f4d;      --vermillion: #c1432a;      --bamboo: #5e7a3c;      --gold: #ab8636;      font-family: 'Zen Kaku Gothic New', sans-serif;      background: var(--paper);      color: var(--ink);      min-height: 100vh;      background-image:        radial-gradient(circle at 1px 1px, rgba(34,37,43,0.06) 1px, transparent 0);      background-size: 18px 18px;    }    .adharav-root *:focus-visible {      outline: 2px solid var(--vermillion);      outline-offset: 2px;    }    .disp { font-family: 'Shippori Mincho', serif; }    .mono { font-family: 'JetBrains Mono', monospace; }    @media (prefers-reduced-motion: reduce) {      .adharav-root * { animation: none !important; transition: none !important; }    }    .adharav-root ::-webkit-scrollbar { width: 8px; height: 8px; }    .adharav-root ::-webkit-scrollbar-thumb { background: #c8bea6; border-radius: 4px; }    @keyframes stampIn {      0% { transform: scale(2.2) rotate(-14deg); opacity: 0; }      60% { transform: scale(0.9) rotate(-6deg); opacity: 1; }      100% { transform: scale(1) rotate(-6deg); opacity: 1; }    }    .stamp-anim { animation: stampIn 0.4s ease-out; }  `}</style>);/* ------------------------------------------------------------------ *//* Claude API helper                                                   *//* ------------------------------------------------------------------ */const API_KEY = import.meta.env.VITE_ANTHROPIC_API_KEY || "";async function askClaude(system, messages) {  if (!API_KEY) {    throw new Error("API key not configured. Please set VITE_ANTHROPIC_API_KEY in your .env file.");  }  const res = await fetch("https://api.anthropic.com/v1/messages", {    method: "POST",    headers: {      "Content-Type": "application/json",      "x-api-key": API_KEY,      "anthropic-version": "2023-06-01",    },    body: JSON.stringify({      model: "claude-sonnet-4-20250514",      max_tokens: 1000,      system,      messages,    }),  });  const data = await res.json();  if (!res.ok) {    throw new Error(data.error?.message || `API error: ${res.status}`);  }  const text = (data.content || [])    .filter((b) => b.type === "text")    .map((b) => b.text)    .join("\n");  return text;}function parseJSONLoose(text) {  const cleaned = text.replace(/```json/gi, "").replace(/```/g, "").trim();  const start = cleaned.indexOf("{");  const startArr = cleaned.indexOf("[");  let s = start;  if (startArr !== -1 && (start === -1 || startArr < start)) s = startArr;  const candidate = s >= 0 ? cleaned.slice(s) : cleaned;  try {    return JSON.parse(candidate);  } catch (e) {    try {      return JSON.parse(cleaned);    } catch (e2) {      return null;    }  }}/* ------------------------------------------------------------------ *//* Speech helpers                                                      *//* ------------------------------------------------------------------ */function speakJapanese(text) {  if (!("speechSynthesis" in window) || !text) return;  window.speechSynthesis.cancel();  const utter = new SpeechSynthesisUtterance(text);  utter.lang = "ja-JP";  utter.rate = 0.92;  const voices = window.speechSynthesis.getVoices();  const jpVoice = voices.find((v) => v.lang && v.lang.startsWith("ja"));  if (jpVoice) utter.voice = jpVoice;  window.speechSynthesis.speak(utter);}function useSpeechRecognition(onResult) {  const recRef = useRef(null);  const [listening, setListening] = useState(false);  const [supported, setSupported] = useState(true);  useEffect(() => {    const SR = window.SpeechRecognition || window.webkitSpeechRecognition;    if (!SR) {      setSupported(false);      return;    }    const rec = new SR();    rec.lang = "ja-JP";    rec.interimResults = false;    rec.maxAlternatives = 1;    rec.onresult = (e) => {      const transcript = e.results[0][0].transcript;      onResult(transcript);    };    rec.onend = () => setListening(false);    rec.onerror = () => setListening(false);    recRef.current = rec;  }, [onResult]);  const start = () => {    if (!recRef.current) return;    try {      recRef.current.start();      setListening(true);    } catch (e) {      /* already started */    }  };  const stop = () => {    if (recRef.current) recRef.current.stop();    setListening(false);  };  return { start, stop, listening, supported };}/* ------------------------------------------------------------------ *//* Static data: kana + preset decks                                    *//* ------------------------------------------------------------------ */const HIRAGANA = [  ["あ", "a", "あさ — asa — morning"], ["い", "i", "いぬ — inu — dog"], ["う", "u", "うみ — umi — sea"], ["え", "e", "えき — eki — station"], ["お", "o", "おちゃ — ocha — tea"],  ["か", "ka", "かさ — kasa — umbrella"], ["き", "ki", "きた — kita — north"], ["く", "ku", "くるま — kuruma — car"], ["け", "ke", "けさ — kesa — this morning"], ["こ", "ko", "こども — kodomo — child"],  ["さ", "sa", "さかな — sakana — fish"], ["し", "shi", "しろ — shiro — white"], ["す", "su", "すし — sushi — sushi"], ["せ", "se", "せかい — sekai — world"], ["そ", "so", "そら — sora — sky"],  ["た", "ta", "たまご — tamago — egg"], ["ち", "chi", "ちず — chizu — map"], ["つ", "tsu", "つき — tsuki — moon"], ["て", "te", "てがみ — tegami — letter"], ["と", "to", "とり — tori — bird"],  ["な", "na", "なつ — natsu — summer"], ["に", "ni", "にく — niku — meat"], ["ぬ", "nu", "ぬの — nuno — cloth"], ["ね", "ne", "ねこ — neko — cat"], ["の", "no", "のみもの — nomimono — drink"],  ["は", "ha", "はな — hana — flower"], ["ひ", "hi", "ひと — hito — person"], ["ふ", "fu", "ふゆ — fuyu — winter"], ["へ", "he", "へや — heya — room"], ["ほ", "ho", "ほし — hoshi — star"],  ["ま", "ma", "まど — mado — window"], ["み", "mi", "みず — mizu — water"], ["む", "mu", "むし — mushi — insect"], ["め", "me", "め — me — eye"], ["も", "mo", "もり — mori — forest"],  ["や", "ya", "やま — yama — mountain"], ["ゆ", "yu", "ゆき — yuki — snow"], ["よ", "yo", "よる — yoru — night"],  ["ら", "ra", "らいねん — rainen — next year"], ["り", "ri", "りんご — ringo — apple"], ["る", "ru", "るす — rusu — absence"], ["れ", "re", "れきし — rekishi — history"], ["ろ", "ro", "ろうか — rouka — hallway"],  ["わ", "wa", "わたし — watashi — I / me"], ["を", "wo", "ほんをよむ — hon wo yomu — read a book"], ["ん", "n", "ほん — hon — book"],];const KATAKANA = [  ["ア", "a", "アメリカ — amerika — America"], ["イ", "i", "イギリス — igirisu — England"], ["ウ", "u", "ウール — ūru — wool"], ["エ", "e", "エレベーター — erebētā — elevator"], ["オ", "o", "オレンジ — orenji — orange"],  ["カ", "ka", "カメラ — kamera — camera"], ["キ", "ki", "キス — kisu — kiss"], ["ク", "ku", "クラス — kurasu — class"], ["ケ", "ke", "ケーキ — kēki — cake"], ["コ", "ko", "コーヒー — kōhī — coffee"],  ["サ", "sa", "サラダ — sarada — salad"], ["シ", "shi", "シャツ — shatsu — shirt"], ["ス", "su", "スープ — sūpu — soup"], ["セ", "se", "セーター — sētā — sweater"], ["ソ", "so", "ソファ — sofa — sofa"],  ["タ", "ta", "タクシー — takushī — taxi"], ["チ", "chi", "チーズ — chīzu — cheese"], ["ツ", "tsu", "ツアー — tsuā — tour"], ["テ", "te", "テレビ — terebi — television"], ["ト", "to", "トマト — tomato — tomato"],  ["ナ", "na", "ナイフ — naifu — knife"], ["ニ", "ni", "ニュース — nyūsu — news"], ["ヌ", "nu", "ヌードル — nūdoru — noodle"], ["ネ", "ne", "ネクタイ — nekutai — necktie"], ["ノ", "no", "ノート — nōto — notebook"],  ["ハ", "ha", "ハム — hamu — ham"], ["ヒ", "hi", "ヒーター — hītā — heater"], ["フ", "fu", "フォーク — fōku — fork"], ["ヘ", "he", "ヘリコプター — herikoputā — helicopter"], ["ホ", "ho", "ホテル — hoteru — hotel"],  ["マ", "ma", "マップ — mappu — map"], ["ミ", "mi", "ミルク — miruku — milk"], ["ム", "mu", "ムード — mūdo — mood"], ["メ", "me", "メニュー — menyū — menu"], ["モ", "mo", "モデル — moderu — model"],  ["ヤ", "ya", "タイヤ — taiya — tire"], ["ユ", "yu", "ユーモア — yūmoa — humor"], ["ヨ", "yo", "ヨガ — yoga — yoga"],  ["ラ", "ra", "ラジオ — rajio — radio"], ["リ", "ri", "リスト — risuto — list"], ["ル", "ru", "ルール — rūru — rule"], ["レ", "re", "レストラン — resutoran — restaurant"], ["ロ", "ro", "ロボット — robotto — robot"],  ["ワ", "wa", "ワイン — wain — wine"], ["ヲ", "wo", "（ほぼ使わない） — rarely used alone"], ["ン", "n", "パン — pan — bread"],];const PRESET_DECKS = {  greetings: {    label: "Greetings", icon: "挨",    cards: [      ["こんにちは", "konnichiwa", "hello / good afternoon"],      ["おはよう ございます", "ohayō gozaimasu", "good morning (polite)"],      ["こんばんは", "konbanwa", "good evening"],      ["さようなら", "sayōnara", "goodbye"],      ["ありがとう ございます", "arigatō gozaimasu", "thank you (polite)"],      ["すみません", "sumimasen", "excuse me / sorry"],      ["はじめまして", "hajimemashite", "nice to meet you"],      ["おげんき ですか", "o-genki desu ka", "how are you?"],      ["はい", "hai", "yes"],      ["いいえ", "iie", "no"],    ],  },  food: {    label: "Food", icon: "食",    cards: [      ["ごはん", "gohan", "rice / meal"],      ["みず", "mizu", "water"],      ["おいしい", "oishii", "delicious"],      ["レストラン", "resutoran", "restaurant"],      ["メニュー", "menyū", "menu"],      ["おかいけい", "o-kaikei", "the check / bill"],      ["すきです", "suki desu", "I like it"],      ["たべます", "tabemasu", "to eat"],      ["のみます", "nomimasu", "to drink"],      ["やさい", "yasai", "vegetables"],    ],  },  travel: {    label: "Travel", icon: "旅",    cards: [      ["えき", "eki", "station"],      ["くうこう", "kūkō", "airport"],      ["ホテル", "hoteru", "hotel"],      ["きっぷ", "kippu", "ticket"],      ["どこですか", "doko desu ka", "where is it?"],      ["みぎ", "migi", "right"],      ["ひだり", "hidari", "left"],      ["タクシー", "takushī", "taxi"],      ["でんしゃ", "densha", "train"],      ["パスポート", "pasupōto", "passport"],    ],  },  numbers: {    label: "Numbers", icon: "数",    cards: [      ["いち", "ichi", "one"],      ["に", "ni", "two"],      ["さん", "san", "three"],      ["よん", "yon", "four"],      ["ご", "go", "five"],      ["ろく", "roku", "six"],      ["なな", "nana", "seven"],      ["はち", "hachi", "eight"],      ["きゅう", "kyū", "nine"],      ["じゅう", "jū", "ten"],    ],  },};const SCENARIOS = [  { id: "coffee", label: "Order coffee", jp: "喫茶店" },  { id: "meet", label: "Meet someone", jp: "自己紹介" },  { id: "airport", label: "At the airport", jp: "空港" },  { id: "restaurant", label: "At a restaurant", jp: "レストラン" },  { id: "directions", label: "Asking directions", jp: "道案内" },  { id: "free", label: "Free chat", jp: "自由会話" },];/* ------------------------------------------------------------------ *//* Persistence                                                         *//* ------------------------------------------------------------------ */const PROGRESS_KEY = "adharav-jp-progress";function todayStr() {  return new Date().toISOString().slice(0, 10);}function computeStreak(prev) {  const today = todayStr();  if (!prev) return { streak: 1, lastActiveDate: today, learnedWords: [] };  if (prev.lastActiveDate === today) return prev;  const yesterday = new Date(Date.now() - 86400000).toISOString().slice(0, 10);  const streak = prev.lastActiveDate === yesterday ? prev.streak + 1 : 1;  return { ...prev, streak, lastActiveDate: today };}/* ------------------------------------------------------------------ *//* Small shared UI bits                                                *//* ------------------------------------------------------------------ */const Seal = ({ n }) => (  <div    className="stamp-anim"    style={{      width: 46, height: 46, borderRadius: 8, border: "2.5px solid var(--vermillion)",      color: "var(--vermillion)", display: "flex", alignItems: "center", justifyContent: "center",      fontFamily: "'Shippori Mincho', serif", fontWeight: 800, fontSize: 15,      transform: "rotate(-6deg)", flexShrink: 0, background: "rgba(193,67,42,0.06)",    }}    title={`${n} day streak`}  >    {n}  </div>);const SpeakerBtn = ({ text, size = 15 }) => (  <button    onClick={() => speakJapanese(text)}    aria-label="Play pronunciation"    style={{      border: "none", background: "var(--indigo)", color: "#fff", borderRadius: "50%",      width: size + 20, height: size + 20, cursor: "pointer", display: "inline-flex",      alignItems: "center", justifyContent: "center", fontSize: size, flexShrink: 0,    }}  >    🔊  </button>);/* ------------------------------------------------------------------ *//* Tab: CHAT                                                            *//* ------------------------------------------------------------------ */function ChatTab({ level, progress, markWordsExposure }) {  const [scenario, setScenario] = useState("free");  const [messages, setMessages] = useState([    {      role: "assistant",      jp: "こんにちは！わたしはアダラブです。にほんごの せんせいです。",      romaji: "Konnichiwa! Watashi wa Adharabu desu. Nihongo no sensei desu.",      en: "Hello! I'm Adharav. I'm your Japanese teacher.",      correction: null,    },  ]);  const [input, setInput] = useState("");  const [loading, setLoading] = useState(false);  const scrollRef = useRef(null);  const handleTranscript = useCallback((t) => setInput((prev) => (prev ? prev + " " + t : t)), []);  const { start, stop, listening, supported } = useSpeechRecognition(handleTranscript);  useEffect(() => {    if (scrollRef.current) scrollRef.current.scrollTop = scrollRef.current.scrollHeight;  }, [messages, loading]);  const send = async (overrideText) => {    const text = (overrideText ?? input).trim();    if (!text || loading) return;    const scenarioObj = SCENARIOS.find((s) => s.id === scenario);    const userMsg = { role: "user", text };    setMessages((m) => [...m, userMsg]);    setInput("");    setLoading(true);    const system = `You are Adharav, a warm, encouraging Japanese language tutor speaking with a ${level} level student.Scenario context: ${scenarioObj.label} (${scenarioObj.jp}). Stay in character for this scenario unless it is "Free chat".Rules:- Reply mostly in Japanese, at ${level} difficulty (very short, simple sentences and common words if Beginner).- If the student's Japanese has a mistake, gently correct it.- Respond ONLY with raw JSON, no markdown fences, no preamble, in exactly this shape:{"reply_jp":"...","reply_romaji":"...","reply_en":"...","correction":null}If there is a mistake to correct, instead of null use:{"original":"what the student wrote","corrected":"the fixed version","note":"one short English sentence explaining why"}Keep reply_jp to 1-2 short sentences and always end it with a simple question to keep the conversation going.`;    const history = messages      .filter((m) => m.role === "user" || m.role === "assistant")      .map((m) =>        m.role === "user"          ? { role: "user", content: m.text }          : { role: "assistant", content: m.jp }      );    try {      const raw = await askClaude(system, [...history, { role: "user", content: text }]);      const parsed = parseJSONLoose(raw);      if (parsed && parsed.reply_jp) {        setMessages((m) => [...m, { role: "assistant", ...parsed }]);        markWordsExposure();      } else {        setMessages((m) => [          ...m,          { role: "assistant", jp: raw, romaji: "", en: "", correction: null },        ]);      }    } catch (e) {      setMessages((m) => [        ...m,        { role: "assistant", jp: "エラーが おきました。", romaji: "Erā ga okimashita.", en: "An error occurred — please try again.", correction: null },      ]);    } finally {      setLoading(false);    }  };  return (    <div style={{ display: "flex", flexDirection: "column", height: "100%" }}>      <div style={{ display: "flex", gap: 8, flexWrap: "wrap", padding: "12px 16px", borderBottom: "1px solid var(--paper-dim)" }}>        {SCENARIOS.map((s) => (          <button            key={s.id}            onClick={() => setScenario(s.id)}            style={{              padding: "6px 12px", borderRadius: 20, cursor: "pointer", fontSize: 13,              border: scenario === s.id ? "2px solid var(--vermillion)" : "1px solid var(--ink-soft)",              background: scenario === s.id ? "rgba(193,67,42,0.08)" : "transparent",              color: "var(--ink)", fontWeight: scenario === s.id ? 700 : 400,            }}          >            {s.label}          </button>        ))}      </div>      <div ref={scrollRef} style={{ flex: 1, overflowY: "auto", padding: 16, display: "flex", flexDirection: "column", gap: 14 }}>        {messages.map((m, i) => (          <div key={i} style={{ display: "flex", justifyContent: m.role === "user" ? "flex-end" : "flex-start" }}>            {m.role === "user" ? (              <div style={{ maxWidth: "78%", background: "var(--indigo)", color: "#fff", padding: "10px 14px", borderRadius: "14px 14px 2px 14px" }}>                {m.text}              </div>            ) : (              <div style={{ maxWidth: "85%", display: "flex", flexDirection: "column", gap: 6 }}>                <div style={{ background: "#fff", border: "1px solid var(--paper-dim)", borderRadius: "2px 14px 14px 14px", padding: "12px 14px" }}>                  <div style={{ display: "flex", alignItems: "center", gap: 8 }}>                    <span className="disp" style={{ fontSize: 19, fontWeight: 700 }}>{m.jp}</span>                    {m.jp && <SpeakerBtn text={m.jp} size={13} />}                  </div>                  {m.romaji && <div className="mono" style={{ fontSize: 12.5, color: "var(--gold)", marginTop: 4 }}>{m.romaji}</div>}                  {m.en && <div style={{ fontSize: 13.5, color: "var(--ink-soft)", marginTop: 2 }}>{m.en}</div>}                </div>                {m.correction && (                  <div style={{ border: "1.5px dashed var(--vermillion)", borderRadius: 10, padding: "8px 12px", fontSize: 13, background: "rgba(193,67,42,0.05)" }}>                    <div style={{ fontWeight: 700, color: "var(--vermillion)", marginBottom: 4 }}>訂正 · Correction</div>                    <div><s style={{ color: "var(--ink-soft)" }}>{m.correction.original}</s></div>                    <div style={{ color: "var(--bamboo)", fontWeight: 600 }}>{m.correction.corrected}</div>                    <div style={{ color: "var(--ink-soft)", marginTop: 3 }}>{m.correction.note}</div>                  </div>                )}              </div>            )}          </div>        ))}        {loading && <div style={{ color: "var(--ink-soft)", fontSize: 13 }}>アダラブが 入力中… (Adharav is typing…)</div>}      </div>      <div style={{ display: "flex", gap: 8, padding: 12, borderTop: "1px solid var(--paper-dim)" }}>        {supported && (          <button            onClick={listening ? stop : start}            aria-label="Speak in Japanese"            style={{              border: "none", borderRadius: "50%", width: 44, height: 44, flexShrink: 0, cursor: "pointer",              background: listening ? "var(--vermillion)" : "var(--indigo)", color: "#fff", fontSize: 18,            }}          >            {listening ? "●" : "🎤"}          </button>        )}        <input          value={input}          onChange={(e) => setInput(e.target.value)}          onKeyDown={(e) => e.key === "Enter" && send()}          placeholder="日本語で書いてね… (write in Japanese)"          style={{ flex: 1, padding: "10px 14px", borderRadius: 22, border: "1px solid var(--ink-soft)", fontSize: 14, background: "#fff" }}        />        <button          onClick={() => send()}          disabled={loading}          style={{ border: "none", borderRadius: 22, padding: "0 20px", background: "var(--vermillion)", color: "#fff", fontWeight: 700, cursor: "pointer" }}        >          送る        </button>      </div>    </div>  );}/* ------------------------------------------------------------------ *//* Tab: ALPHABET                                                        *//* ------------------------------------------------------------------ */function AlphabetTab() {  const [set, setSet] = useState("hiragana");  const [selected, setSelected] = useState(null);  const data = set === "hiragana" ? HIRAGANA : KATAKANA;  const pick = (entry) => {    setSelected(entry);    speakJapanese(entry[0]);  };  return (    <div style={{ padding: 16 }}>      <div style={{ display: "flex", gap: 8, marginBottom: 16 }}>        {["hiragana", "katakana"].map((k) => (          <button            key={k}            onClick={() => { setSet(k); setSelected(null); }}            style={{              padding: "8px 16px", borderRadius: 20, cursor: "pointer", fontWeight: 700, fontSize: 13,              border: set === k ? "2px solid var(--vermillion)" : "1px solid var(--ink-soft)",              background: set === k ? "rgba(193,67,42,0.08)" : "transparent",              textTransform: "capitalize",            }}          >            {k === "hiragana" ? "ひらがな Hiragana" : "カタカナ Katakana"}          </button>        ))}      </div>      {selected && (        <div style={{ display: "flex", alignItems: "center", gap: 14, background: "#fff", border: "1px solid var(--paper-dim)", borderRadius: 14, padding: 16, marginBottom: 16 }}>          <span className="disp" style={{ fontSize: 48, fontWeight: 800, color: "var(--indigo)" }}>{selected[0]}</span>          <div>            <div className="mono" style={{ fontSize: 14, color: "var(--gold)" }}>{selected[1]}</div>            <div style={{ fontSize: 14, marginTop: 2 }}>{selected[2]}</div>          </div>          <SpeakerBtn text={selected[0]} />        </div>      )}      <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(56px, 1fr))", gap: 8 }}>        {data.map((entry) => (          <button            key={entry[0]}            onClick={() => pick(entry)}            className="disp"            style={{              aspectRatio: "1", fontSize: 22, fontWeight: 700, borderRadius: 10, cursor: "pointer",              border: selected && selected[0] === entry[0] ? "2px solid var(--vermillion)" : "1px solid var(--paper-dim)",              background: "#fff", color: "var(--ink)",            }}          >            {entry[0]}          </button>        ))}      </div>    </div>  );}/* ------------------------------------------------------------------ *//* Tab: VOCAB                                                           *//* ------------------------------------------------------------------ */function Flashcard({ card, id, learned, onToggleLearned }) {  const [flipped, setFlipped] = useState(false);  const [word, romaji, meaning] = card;  return (    <div      onClick={() => setFlipped((f) => !f)}      style={{        border: learned ? "2px solid var(--bamboo)" : "1px solid var(--paper-dim)",        borderRadius: 14, background: "#fff", padding: 18, cursor: "pointer",        minHeight: 120, display: "flex", flexDirection: "column", justifyContent: "center",        position: "relative",      }}    >      {learned && <span style={{ position: "absolute", top: 8, right: 10, fontSize: 12, color: "var(--bamboo)", fontWeight: 700 }}>✓ 覚えた</span>}      {!flipped ? (        <div style={{ textAlign: "center" }}>          <div className="disp" style={{ fontSize: 24, fontWeight: 700 }}>{word}</div>          <div className="mono" style={{ fontSize: 12, color: "var(--gold)", marginTop: 6 }}>{romaji}</div>        </div>      ) : (        <div style={{ textAlign: "center" }}>          <div style={{ fontSize: 16, fontWeight: 600 }}>{meaning}</div>        </div>      )}      <div style={{ display: "flex", justifyContent: "center", gap: 10, marginTop: 12 }} onClick={(e) => e.stopPropagation()}>        <SpeakerBtn text={word} size={12} />        <button          onClick={() => onToggleLearned(id)}          style={{            fontSize: 11, border: "1px solid var(--bamboo)", color: learned ? "#fff" : "var(--bamboo)",            background: learned ? "var(--bamboo)" : "transparent", borderRadius: 14, padding: "4px 10px", cursor: "pointer",          }}        >          {learned ? "覚えた" : "覚える"}        </button>      </div>    </div>  );}function VocabTab({ progress, toggleLearned }) {  const [deckKey, setDeckKey] = useState("greetings");  const [customTopic, setCustomTopic] = useState("");  const [customDeck, setCustomDeck] = useState(null);  const [loadingDeck, setLoadingDeck] = useState(false);  const [customError, setCustomError] = useState("");  const deck = deckKey === "custom" ? customDeck : PRESET_DECKS[deckKey];  const generateDeck = async () => {    if (!customTopic.trim() || loadingDeck) return;    setLoadingDeck(true);    setCustomError("");    const system = `You generate Japanese vocabulary flashcards for a language learner.Respond ONLY with raw JSON (no markdown fences), an array of exactly 10 items, each shaped:{"word":"Japanese word or short phrase","romaji":"romanized reading","meaning":"short English meaning"}`;    try {      const raw = await askClaude(system, [{ role: "user", content: `Topic: ${customTopic}` }]);      const parsed = parseJSONLoose(raw);      if (Array.isArray(parsed) && parsed.length) {        setCustomDeck({          label: customTopic, icon: "話",          cards: parsed.map((c) => [c.word, c.romaji, c.meaning]),        });        setDeckKey("custom");      } else {        setCustomError("Could not generate a deck — try a different topic.");      }    } catch (e) {      setCustomError("Something went wrong generating the deck.");    } finally {      setLoadingDeck(false);    }  };  return (    <div style={{ padding: 16 }}>      <div style={{ display: "flex", gap: 8, flexWrap: "wrap", marginBottom: 12 }}>        {Object.entries(PRESET_DECKS).map(([k, v]) => (          <button            key={k}            onClick={() => setDeckKey(k)}            style={{              padding: "7px 14px", borderRadius: 20, cursor: "pointer", fontSize: 13, fontWeight: 700,              border: deckKey === k ? "2px solid var(--vermillion)" : "1px solid var(--ink-soft)",              background: deckKey === k ? "rgba(193,67,42,0.08)" : "transparent",            }}          >            {v.icon} {v.label}          </button>        ))}        {customDeck && (          <button            onClick={() => setDeckKey("custom")}            style={{              padding: "7px 14px", borderRadius: 20, cursor: "pointer", fontSize: 13, fontWeight: 700,              border: deckKey === "custom" ? "2px solid var(--vermillion)" : "1px solid var(--ink-soft)",              background: deckKey === "custom" ? "rgba(193,67,42,0.08)" : "transparent",            }}          >            話 {customDeck.label}          </button>        )}      </div>      <div style={{ display: "flex", gap: 8, marginBottom: 20 }}>        <input          value={customTopic}          onChange={(e) => setCustomTopic(e.target.value)}          onKeyDown={(e) => e.key === "Enter" && generateDeck()}          placeholder="Type any topic — e.g. weather, at the gym…"          style={{ flex: 1, padding: "9px 14px", borderRadius: 20, border: "1px solid var(--ink-soft)", fontSize: 13, background: "#fff" }}        />        <button          onClick={generateDeck}          disabled={loadingDeck}          style={{ border: "none", borderRadius: 20, padding: "0 18px", background: "var(--indigo)", color: "#fff", fontWeight: 700, cursor: "pointer" }}        >          {loadingDeck ? "作成中…" : "Generate"}        </button>      </div>      {customError && <div style={{ color: "var(--vermillion)", fontSize: 13, marginBottom: 12 }}>{customError}</div>}      {deck && (        <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(150px, 1fr))", gap: 12 }}>          {deck.cards.map((card, i) => {            const id = `${deckKey === "custom" ? "custom-" + customTopic : deckKey}-${i}`;            return (              <Flashcard                key={id}                id={id}                card={card}                learned={progress.learnedWords.includes(id)}                onToggleLearned={toggleLearned}              />            );          })}        </div>      )}    </div>  );}/* ------------------------------------------------------------------ *//* Tab: TRANSLATE                                                       *//* ------------------------------------------------------------------ */function TranslateTab({ level }) {  const [input, setInput] = useState("");  const [result, setResult] = useState(null);  const [loading, setLoading] = useState(false);  const [error, setError] = useState("");  const translate = async () => {    if (!input.trim() || loading) return;    setLoading(true);    setError("");    setResult(null);    const system = `You are a Japanese-English translator and grammar teacher for a ${level} level student.Detect if the input is English or Japanese. Translate it to the other language.Respond ONLY with raw JSON (no markdown fences), shaped exactly:{"input_lang":"en" or "ja","translation":"the translation","romaji":"romanized reading of the Japanese (input or output, whichever is Japanese)","breakdown":[{"word":"...","reading":"...","meaning":"...","grammar":"short grammar note, e.g. particle, past tense, verb"}]}The breakdown must cover the Japanese sentence word by word, in order.`;    try {      const raw = await askClaude(system, [{ role: "user", content: input }]);      const parsed = parseJSONLoose(raw);      if (parsed && parsed.translation) {        setResult(parsed);      } else {        setError("Could not parse a translation — try rephrasing.");      }    } catch (e) {      setError("Something went wrong translating that.");    } finally {      setLoading(false);    }  };  return (    <div style={{ padding: 16 }}>      <textarea        value={input}        onChange={(e) => setInput(e.target.value)}        placeholder="Type in English or Japanese…"        rows={3}        style={{ width: "100%", padding: 12, borderRadius: 12, border: "1px solid var(--ink-soft)", fontSize: 15, resize: "vertical", background: "#fff" }}      />      <button        onClick={translate}        disabled={loading}        style={{ marginTop: 10, border: "none", borderRadius: 20, padding: "9px 22px", background: "var(--vermillion)", color: "#fff", fontWeight: 700, cursor: "pointer" }}      >        {loading ? "翻訳中…" : "Translate"}      </button>      {error && <div style={{ color: "var(--vermillion)", fontSize: 13, marginTop: 10 }}>{error}</div>}      {result && (        <div style={{ marginTop: 20 }}>          <div style={{ background: "#fff", border: "1px solid var(--paper-dim)", borderRadius: 14, padding: 16, marginBottom: 16 }}>            <div style={{ display: "flex", alignItems: "center", gap: 10 }}>              <span className="disp" style={{ fontSize: 21, fontWeight: 700 }}>{result.translation}</span>              {result.input_lang === "en" && <SpeakerBtn text={result.translation} size={13} />}            </div>            {result.romaji && <div className="mono" style={{ fontSize: 13, color: "var(--gold)", marginTop: 6 }}>{result.romaji}</div>}          </div>          {Array.isArray(result.breakdown) && result.breakdown.length > 0 && (            <div>              <div className="disp" style={{ fontWeight: 700, marginBottom: 8, fontSize: 15 }}>分解 · Word by word</div>              <div style={{ display: "flex", flexDirection: "column", gap: 6 }}>                {result.breakdown.map((w, i) => (                  <div key={i} style={{ display: "grid", gridTemplateColumns: "1fr 1fr 1.4fr 1.4fr", gap: 10, background: "#fff", border: "1px solid var(--paper-dim)", borderRadius: 10, padding: "8px 12px", fontSize: 13, alignItems: "center" }}>                    <span className="disp" style={{ fontWeight: 700 }}>{w.word}</span>                    <span className="mono" style={{ color: "var(--gold)" }}>{w.reading}</span>                    <span>{w.meaning}</span>                    <span style={{ color: "var(--ink-soft)", fontSize: 12 }}>{w.grammar}</span>                  </div>                ))}              </div>            </div>          )}        </div>      )}    </div>  );}/* ------------------------------------------------------------------ *//* App shell                                                            *//* ------------------------------------------------------------------ */const TABS = [  { id: "chat", label: "Chat", jp: "会話" },  { id: "alphabet", label: "Alphabet", jp: "文字" },  { id: "vocab", label: "Vocab", jp: "単語" },  { id: "translate", label: "Translate", jp: "翻訳" },];export default function AdharavApp() {  const [tab, setTab] = useState("chat");  const [level, setLevel] = useState("Beginner");  const [progress, setProgress] = useState({ streak: 1, lastActiveDate: todayStr(), learnedWords: [] });  const [ready, setReady] = useState(false);  useEffect(() => {    (async () => {      let stored = null;      try {        const res = await localStorage.getItem(PROGRESS_KEY);        if (res) stored = JSON.parse(res.value);      } catch (e) {        stored = null;      }      const updated = computeStreak(stored);      setProgress(updated);      setReady(true);      try {        await localStorage.setItem(PROGRESS_KEY, JSON.stringify(updated));      } catch (e) {}    })();  }, []);  const saveProgress = async (next) => {    setProgress(next);    try {      await localStorage.setItem(PROGRESS_KEY, JSON.stringify(next));    } catch (e) {}  };  const toggleLearned = (id) => {    setProgress((prev) => {      const has = prev.learnedWords.includes(id);      const learnedWords = has ? prev.learnedWords.filter((w) => w !== id) : [...prev.learnedWords, id];      const next = { ...prev, learnedWords };      saveProgress(next);      return next;    });  };  const markWordsExposure = () => {};  if (!ready) {    return (      <div className="adharav-root" style={{ display: "flex", alignItems: "center", justifyContent: "center", height: "100vh" }}>        <GlobalStyle />        <div className="disp" style={{ fontSize: 20, color: "var(--ink-soft)" }}>読み込み中…</div>      </div>    );  }  return (    <div className="adharav-root" style={{ display: "flex", height: "100vh", overflow: "hidden" }}>      <GlobalStyle />      {/* Side tab rail */}      <div style={{ width: 84, background: "var(--indigo-deep)", display: "flex", flexDirection: "column", alignItems: "center", padding: "20px 0", flexShrink: 0 }}>        <div className="disp" style={{ color: "#fff", fontWeight: 800, fontSize: 22, marginBottom: 28, writingMode: "vertical-rl" }}>アダラブ</div>        {TABS.map((t) => (          <button            key={t.id}            onClick={() => setTab(t.id)}            style={{              width: 60, marginBottom: 10, padding: "10px 0", borderRadius: 10, cursor: "pointer",              border: "none", background: tab === t.id ? "var(--vermillion)" : "transparent",              color: tab === t.id ? "#fff" : "#c8d2e0", display: "flex", flexDirection: "column", alignItems: "center", gap: 3,            }}          >            <span className="disp" style={{ fontSize: 18, fontWeight: 700 }}>{t.jp}</span>            <span style={{ fontSize: 9.5 }}>{t.label}</span>          </button>        ))}      </div>      {/* Main area */}      <div style={{ flex: 1, display: "flex", flexDirection: "column", minWidth: 0 }}>        <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", padding: "14px 20px", borderBottom: "1px solid var(--paper-dim)" }}>          <div>            <div className="disp" style={{ fontSize: 20, fontWeight: 800 }}>Adharav — 日本語の先生</div>            <div style={{ fontSize: 12, color: "var(--ink-soft)" }}>Your Japanese tutor</div>          </div>          <div style={{ display: "flex", alignItems: "center", gap: 16 }}>            <div style={{ textAlign: "right" }}>              <div style={{ fontSize: 11, color: "var(--ink-soft)" }}>覚えた単語 · learned</div>              <div style={{ fontWeight: 700, fontSize: 15 }}>{progress.learnedWords.length}</div>            </div>            <Seal n={progress.streak} />            <select              value={level}              onChange={(e) => setLevel(e.target.value)}              style={{ padding: "6px 10px", borderRadius: 8, border: "1px solid var(--ink-soft)", background: "#fff", fontSize: 13 }}            >              <option>Beginner</option>              <option>Intermediate</option>              <option>Advanced</option>            </select>          </div>        </div>        <div style={{ flex: 1, minHeight: 0, overflowY: tab === "chat" ? "hidden" : "auto" }}>          {tab === "chat" && <ChatTab level={level} progress={progress} markWordsExposure={markWordsExposure} />}          {tab === "alphabet" && <AlphabetTab />}          {tab === "vocab" && <VocabTab progress={progress} toggleLearned={toggleLearned} />}          {tab === "translate" && <TranslateTab level={level} />}        </div>      </div>    </div>  );}



